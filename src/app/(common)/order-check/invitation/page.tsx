import Receipt from "../(components)/Receipt/Receipt";

const page = () => {
  return (
    <div>
      page
      <Receipt title="최종 결제 금액">
        <span>하이</span>
      </Receipt>
    </div>
  );
};

export default page;
